package pages.workPanel.projects;

import org.openqa.selenium.WebDriver;
import pages.workPanel.WorkPanel;

public class ProjectsPage extends WorkPanel {

    public ProjectsPage(WebDriver driver) {
        super(driver);
    }
}
