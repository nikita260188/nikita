package pages.workPanel.security;

import org.openqa.selenium.WebDriver;
import pages.workPanel.WorkPanel;

public class SecurityPage extends WorkPanel {

    public SecurityPage(WebDriver driver) {
        super(driver);
    }
}
