package pages.workPanel.wiki;

import org.openqa.selenium.WebDriver;
import pages.workPanel.WorkPanel;

public class WikiPage extends WorkPanel {

    public WikiPage(WebDriver driver) {
        super(driver);
    }
}
